<h1>HELLO, <%= @user_name %> </h1>

<%= link_to "Login", new_session_path %>
<br>
<%= link_to "Logout", logout_path, method: :delete %>
<br>


<% if @user_name != "Guest" %>
  <%= link_to "My Account Page", user_path(@user_id) %>
<% end %>

<% @products.each do |product| %>
  <% if !product.retired? %>
    <a href="product_route(product.id)"> <%= product.name %> $<%= product.price %> </a>
  <% end %>

<%= link_to(products_path(order: 'prod'), :class => "nav_button") do %>
   <hr />
   by product
   <hr />
<% end %>
<%= link_to(products_path(order: 'cat'), :class => "nav_button") do %>
   <hr />
   by category
   <hr />
<% end %>
<%= link_to(products_path(order: 'mart'), :class => "nav_button") do %>
  <hr />
  by category
  <hr />
<% end %>

<% if @order = "prod" %>
  <% @products.each do |product| %>
    <% if !product.retired? %>
      <a href="product_route(product.id)"> <%= product.name %> $<%= product.price %> </a>
    <% end %>
  <% end %>
<% elsif @order = "cat" %>
  <% @categories.each do |cat| %>
    <% @products.each do |product| %>
      <% if !product.retired? && product.categories.include?(cat) %>
        <a href="product_route(product.id)"> <%= product.name %> $<%= product.price %> </a>
      <% end %>
    <% end %>
  <% end %>
<% else %>
  <% @markets.each do |mark| %>
    <% @products.each do |product| %>
      <% if !product.retired? && product.categories.include?(mark) %>
        <a href="product_route(product.id)"> <%= product.name %> $<%= product.price %> </a>
      <% end %>
    <% end %>
  <% end %>
