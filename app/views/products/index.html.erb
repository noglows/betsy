<h1>HELLO, <%= @user_name %> </h1>

<%= link_to "Login", new_session_path %>
<br>
<%= link_to "Logout", logout_path, method: :delete %>
<br>

<% if @user_name != "Guest" %>
  <%= link_to "My Account Page", user_path(@user_id) %>
<% end %>

<div class="btn-group">
  <%= link_to(products_path(order: 'prod'), :class => "btn btn-default") do %>
     by product
  <% end %>
  <%= link_to(products_path(order: 'cat'), :class => "btn btn-default") do %>
     by category
  <% end %>
  <%= link_to(products_path(order: 'mart'), :class => "btn btn-default") do %>
    by category
  <% end %>
</div>

<br>
<% @sort_by.each do |sort| %>
  <% @products.each do |product| if @sort_by!=nil %>
    <% if !product.retired? && product.sort %>
      <a href="product_route(product.id)"> <%= product.name %> $<%= product.price %> </a>
      <br>
    <% end %>
  <% end if @sort_by!=nil %>
<% end %>

<!-- <% elsif @order = "cat" %>
  <% @categories.each do |cat| %>
    <% @products.each do |product| %>
      <% if !product.retired? && product.categories.include?(cat) %>
        <a href="product_route(product.id)"> <%= product.name %> $<%= product.price %> </a>
        <br>
      <% end %>
    <% end %>
  <% end %>
<% else %>
  <% @markets.each do |mark| %>
    <% @products.each do |product| %>
      <% if !product.retired? && product.categories.include?(mark) %>
        <a href="product_route(product.id)"> <%= product.name %> $<%= product.price %> </a>
        <br>
      <% end %>
    <% end %>
  <% end %>
<% end %> -->
